 /*const targetElement = document.getElementById('problem');
 const textContent = targetElement.textContent;
    const words = textContent.split('problem');

    let newHTML = '';
    words.forEach(word => {
        newHTML += `<span>${problem}</span> `; // Add a space after each word
    });
    targetElement.innerHTML = newHTML;

    // Using GSAP (after including the library)
        gsap.from(targetElement.querySelectorAll('problem'), {
            opacity: 0,
            y: 50,
            stagger: 0.2, // Stagger animation by 0.1 seconds
            duration: 0.8
        });*/
